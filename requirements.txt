# Requirements for Equivariant CNNs for SAR Avalanche Debris Detection
#
# Base environment: NVIDIA PyTorch container pytorch_24.12-py3.sif
#   (PyTorch 2.6.0 + CUDA 12.4, torchvision, numpy 2.2 provided by container)
#
# Install these on top of the container venv (see slurm/setup_venv.sh):
#   pip install -r requirements.txt
#
# Note: torch and torchvision are provided by the container; do not list them
# here or pip will attempt to reinstall a CPU-only wheel.
#
# Note: numpy is pinned <2 because lie-learn (a dependency of escnn) is
# incompatible with the numpy 2.x API changes. The container ships numpy 2.x;
# this pin prevents pip from upgrading it and breaking lie-learn.

# Equivariance
autograd
pymanopt
py3nj
lie-learn==0.0.2
escnn>=0.1.9

# ML / data stack
numpy<2
wandb>=0.17
scikit-learn>=1.3
matplotlib
seaborn
rasterio>=1.3
geopandas>=0.14
sentinelsat
pandas
tifffile
scipy
tqdm
requests
